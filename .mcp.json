{
  "mcpServers": {
    "notion-proseed": {
      "command": "npx",
      "args": ["-y", "@notionhq/notion-mcp-server"],
      "env": {
        "NOTION_TOKEN": "${NOTION_PROSEED_TOKEN}"
      }
    }
  }
}
