apiVersion: v1
kind: ConfigMap
metadata:
  name: web-env
  namespace: proseed
data:
  NEXT_URL: 'https://proseednow.com'
  MEDIA_BUCKET_NAME: 'proseed-media'
  OTEL_EXPORTER_OTLP_ENDPOINT: 'https://otel.proseednow.com'
  # 서버사이드 렌더링용 클러스터 내부 API 주소 (공개 URL 은 Pod 에서 hairpin 실패)
  INTERNAL_API_URL: 'http://api-server.proseed.svc.cluster.local:4000/api'
